#include <stdio.h> 
#include <math.h>

int main ()
{
     printf("the value of 4 to the power 5 is %f\n", pow(4 , 5));
     printf("the value of 4 to the power 5 is %d\n", pow(4 , 5));
     return 0;
}